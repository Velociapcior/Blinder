# Agent Customization
# Customize any section below - all are optional

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

critical_actions:
  - "LOAD and internalize {project-root}/docs/tech-preferences.md"
  - "LOAD and internalize {project-root}/docs/project-context.md"

memories:
  - "Project: Blinder — a blind dating app"
  - "Frontend: React Native with Expo SDK 55"
  - "Backend: ASP.NET Core .NET 10 with C#"
  - "Real-time: SignalR for messaging"
  - "Auth: to be determined (Okta SSO considered)"
  - "Storage: MinIO on self-hosted Hetzner VPS via Docker Compose"
  - "Always follow patterns defined in docs/tech-preferences.md"
  - "Always follow architecture decisions in docs/project-context.md"

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
